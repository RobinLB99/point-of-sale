# =============================================================
# EJEMPLO DE CONFIGURACIÓN DE ENTORNO
# =============================================================

# Perfil de Spring (dev, prod, test)
SPRING_PROFILES_ACTIVE=dev

# Base de Datos
DB_HOST=localhost
DB_PORT=5432
DB_NAME=pos_system
DB_USER=postgres
DB_PASS=password

# JWT / Seguridad
# JWT_SECRET=generate_a_secure_random_string
# JWT_EXPIRATION=3600000
